[project]
name = "slob-games"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.3.5",
    "pytest>=9.0.1",
]

[tool.ruff]
line-length = 88
select = ["D", "E", "F", "I"]

